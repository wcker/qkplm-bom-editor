{
  "name": "@bom-editor/model",
  "version": "0.0.0",
  "private": true,
  "description": "DOM-free validation, normalization, hashing, indexes, and stable-key Diff for BOM snapshots.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @bom-editor/contracts build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @bom-editor/contracts build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test --test-isolation=none test/diff.test.mjs test/hash.test.mjs test/matching.test.mjs test/snapshot-index.test.mjs test/value-schema.test.mjs"
  },
  "dependencies": {
    "@bom-editor/contracts": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  }
}
