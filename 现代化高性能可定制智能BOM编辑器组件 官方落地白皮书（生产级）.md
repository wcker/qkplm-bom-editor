# （已废弃）现代化高性能可定制智能BOM编辑器组件 官方落地白皮书（生产级）

> [!WARNING]
> 文档状态：Superseded，仅保留为历史产品愿景和需求素材。  
> 唯一工程规范源：[现代化高性能可复用 BOM 编辑器组件 · 生产级技术白皮书 v2.0](./现代化高性能可复用BOM编辑器组件.md)。  
> 本文中的五层架构、DOM 渲染、全量 Worker、无上限历史、绝对性能承诺和自动修复规则不得用于研发或验收。生成式 AI 能力进入可选 `@bom-editor/ai` 插件候选范围，须经独立需求、ADR、安全评审和验收标准批准后实施。

## 0\. 前言与产品顶层定位

### 0\.1 文档目的

本文为**从零开发、AI可直接执行、可量产、可验收、可私有化**的企业级通用BOM编辑器组件开发规范白皮书。统一架构标准、性能指标、交互规范、定制化能力、智能能力、工程化要求，作为前端开发、算法落地、Demo验证、上线验收的唯一官方依据。

### 0\.2 产品核心定位

自研无重度第三方依赖的**通用可复用现代化BOM结构化编辑器组件**，彻底解决传统Tree/Table改造BOM编辑器的性能卡顿、交互老旧、定制成本高、无智能能力、操作低效等行业痛点，适配PLM、ERP、MES、OA、工程项目管理等全工业场景。

### 0\.3 四大核心产品特质（核心卖点，全程贯穿所有模块）

- **极致高性能**：十万级BOM节点无卡顿、零主线程阻塞、内存稳定可控

- **极致高响应**：60fps帧级交互、局部增量更新、所有操作无感反馈

- **极致易用现代化**：Excel级操作、全场景可定制、自定义快捷键、零代码可视化配置

- **高阶AI智能化**：全链路BOM智能生成、纠错、校验、优化、结构化重构

### 0\.4 最终交付产物

- 通用可复用组件包（Vue3 / React 双版本，TS全类型、按需加载）

- 独立Mock Demo验证工程（性能压测、定制化演示、AI能力验收、交互校验）

- 完整可生产工程化代码、API文档、验收标准、部署规范

## 1\. 整体架构设计（顶级前端架构师标准）

### 1\.1 五层解耦单向架构（强制规范，禁止跨层耦合）

分层独立职责、单向数据流、可单独单元测试、可独立迭代替换，从底层杜绝性能瓶颈与逻辑混乱。

1. **内核渲染层**：自研树形虚拟滚动、可视区域裁剪、DOM轻量化渲染、布局计算

2. **数据计算层**：WebWorker异步分片计算、BOM树形算法、校验算法、统计算法

3. **状态调度层**：三层状态隔离、增量Diff更新、事务撤销重做、全局缓存调度

4. **交互体验层**：自定义快捷键体系、拖拽层级、原位编辑、批量操作、视图状态缓存

5. **AI智能层**：BOM智能生成、结构化重构、错误校验、结构优化、智能补全

### 1\.2 架构硬性红线（开发零违规）

- 禁止基于传统Element/Antd Tree/Table二次封装，彻底规避原生组件性能天花板

- 所有耗时计算、递归遍历、批量处理**必须WebWorker异步执行**，主线程永久只负责渲染与交互

- DOM节点数量恒定，与BOM总节点数无关，仅渲染可视区域内容

- 所有数据更新采用**最小粒度增量Diff**，禁止全量重渲染、全量数据替换

- 所有用户操作纳入事务系统，支持无上限撤销重做

- AI模块独立分包懒加载，不阻塞首屏渲染，可按需开关

## 2\. 高性能量化规范（可自动化验收、生产级硬指标）

所有性能指标**必须在Mock Demo中可视化、可量化、可复现、可验收**。

### 2\.1 大数据渲染性能指标

- 1万层级BOM节点：滚动、展开折叠、编辑、筛选帧率稳定55–60fps，无卡顿、无闪烁、无白屏

- 5万层级BOM节点：全功能正常使用，UI全程无阻塞、无冻结

- 10万超大节点：分片懒加载，首屏渲染完成≤300ms

- 整层展开/折叠响应耗时≤50ms

- 全量数据关键词筛选、过滤耗时≤80ms（Worker预建索引缓存）

### 2\.2 内存稳定性规范

- 连续高频操作30分钟，内存增量泄漏≤15%，无持续上涨

- 频繁增删节点、切换层级、批量操作，内存无堆积残留

- 组件销毁时，Worker线程、全局缓存、事件监听、定时器、DOM实例全部彻底销毁，无内存残留

### 2\.3 算法计算性能规范

- BOM层级关系、父子映射、深度索引、排序位置全量预计算、全局缓存

- 批量增删改1000条节点，UI全程可交互，无阻塞卡顿

- 循环依赖检测、层级合规校验、数据异常校验全异步分片执行

## 3\. 高响应交互规范（60fps工业级跟手体验）

所有交互严格对齐屏幕16ms刷新帧率，做到**零延迟、无拖影、无等待、无全局刷新**。

### 3\.1 基础响应硬性要求

- 滚动、hover、选中、编辑唤起、弹窗展示响应耗时≤16ms

- 节点拖拽、层级移动实时跟手，无滞后、无拖影、无卡顿

- 原位编辑即时唤起，无加载延迟、无阻塞弹窗

- 筛选、排序、视图切换仅局部刷新，禁止全局重绘

### 3\.2 增量更新核心机制

所有数据变更仅触发最小范围更新，杜绝无效渲染：

- 单节点字段修改：仅更新当前单元格DOM

- 节点层级移动、增删：仅刷新当前节点\+关联子树区域

- 批量操作：自动合并为单次事务，仅执行一次局部渲染更新

### 3\.3 视图状态持久化

- 切换筛选、排序、视图模板后，自动保留滚动位置、选中节点、层级展开/折叠状态

- 页面刷新、重新进入自动还原上一次操作视图状态

## 4\. 现代化易用性全套规范（极简、高效、零学习成本）

### 4\.1 无代码自定义列体系（核心定制能力）

完全支持用户可视化自主配置，无需开发介入，对标商业顶级PLM编辑器能力。

- 基础配置：列显示/隐藏、拖拽排序、自定义列宽、列边界双击自动适应、左右固定列、列重置

- 列头右键菜单：一键隐藏、锁定列、按内容自动调整列宽、字段配置、样式设置

- 自定义业务字段：支持文本、数字、浮点、日期、下拉选择、物料编码、人员选择等全类型字段

- 多套视图模板：支持编辑视图、审核视图、导出视图、极简视图等模板新增、保存、切换、删除

- 持久化策略：支持本地缓存\+云端用户维度持久化

- 导出联动：Excel/PDF导出自动跟随当前可视列、列顺序

### 4\.2 Excel级原位编辑体验

- 双击单元格快速唤起原位编辑

- Tab/Shift\+Tab快速切换相邻单元格

- Enter确认编辑并自动下移聚焦

- 鼠标框选多单元格，支持批量填充、批量修改

### 4\.3 树形结构现代化体验

- 精准层级缩进、层级连接线可视化展示

- 轻量化展开/折叠动画，无卡顿闪烁

- 选中行高亮、父级链路高亮，层级关系一目了然

- 搜索匹配自动展开对应层级、自动定位高亮节点

### 4\.4 组件接入极简性

- 零配置可用，仅传入BOM节点数据即可渲染完整编辑器

- 配置项扁平化、层级清晰，支持全局默认配置\+局部覆盖

- 丰富插槽：自定义列头、单元格、行样式、行操作按钮

- 完整生命周期钩子与事件回调，适配全业务场景扩展

## 5\. 快捷键全套体系（默认快捷键 \+ 全自定义能力）

支持**开发者API自定义 \+ 用户可视化面板自定义**双模式，现代化专业编辑器标配，全覆盖无死角。

### 5\.1 默认内置快捷键（标准工业配置）

#### 5\.1\.1 基础编辑快捷键

- Ctrl\+C / Ctrl\+V：复制节点、粘贴节点（保留原始层级结构）

- Ctrl\+X：剪切选中节点/子树

- Delete：删除选中节点及对应子树

- Ctrl\+Z / Ctrl\+Y：撤销 / 重做（无上限事务栈）

- F2：快速唤起原位编辑

- Enter：确认编辑、快速新增子节点

- Tab / Shift\+Tab：切换上下单元格

#### 5\.1\.2 树形结构操作快捷键

- Ctrl\+↑/↓：上下移动节点位置

- Ctrl\+←/→：节点升级/降级（调整层级）

- \+ / \-：展开/折叠当前子树

- Ctrl\+\+：展开全部层级

- Ctrl\+\-：折叠全部层级

#### 5\.1\.3 批量选择快捷键

- Shift\+点击：区间连续多选

- Ctrl\+点击：零散多点复选

- Ctrl\+A：全选当前可视区域所有节点

### 5\.2 全量快捷键自定义能力（核心新增能力）

#### 5\.2\.1 开发者侧API自定义

组件支持初始化传入shortcuts配置项，可**覆盖、修改、新增、清空**所有快捷键，支持任意组合键（Ctrl/Shift/Alt/Cmd \+ 任意按键）。

#### 5\.2\.2 用户侧可视化自定义（无代码）

- 内置快捷键设置面板，展示所有可操作功能与对应按键

- 支持用户自定义绑定、修改、清空快捷键

- 内置按键冲突检测，重复绑定自动提示拦截

- 支持一键恢复默认快捷键配置

- 配置支持用户级持久化存储

#### 5\.2\.3 智能上下文热键规则

- 仅编辑器聚焦状态下快捷键生效，全局页面无冲突

- 单元格编辑状态自动屏蔽全局快捷键，避免输入冲突

- 弹窗、浮层打开时自动屏蔽对应冲突按键

### 5\.3 快捷键辅助体验

- 悬浮快捷键提示面板，展示全部可用按键，新手零学习成本

- 所有快捷键操作同步纳入事务栈，支持撤销重做

## 6\. AI高阶智能化能力（产品核心差异化壁垒）

区别于传统纯可视化BOM组件，本组件内置**工业级BOM智能推理与优化引擎**，实现全流程自动化、智能化。

### 6\.1 AI智能结构化生成

- 自然语言生成BOM：输入产品需求描述，自动生成3–8级标准BOM层级、物料名称、规格、数量、单位、基础分类

- 扁平数据智能重构：导入杂乱Excel扁平清单，AI自动识别父子关系、重构标准树形BOM结构

- AI智能补全：自动补全缺失规格、材质、单价、损耗率等物料字段信息

### 6\.2 AI智能校验纠错引擎

- 自动检测BOM致命循环依赖，精准定位异常节点

- 识别层级错乱、数量异常、规格不匹配、字段缺失等问题

- 检测重复物料、冗余子树、无效空节点

- 一键智能修复所有异常，自动规整错误结构

### 6\.3 AI智能结构优化与统计

- 自动合并重复子树、清理冗余无效节点

- 自动规整统一层级深度，优化BOM阅读结构

- 智能分类排序：自动区分自制件、外购件、标准件、辅料

- 自动汇总总用量、损耗量、物料总数、层级统计数据

### 6\.4 AI能力工程化规范

- AI模块独立分包懒加载，不影响基础组件性能

- 支持开关控制，低配设备可手动关闭智能能力

- 所有AI运算异步执行，不阻塞UI交互

## 7\. 核心底层自研算法（算法工程师落地标准）

### 7\.1 树形虚拟滚动算法

自研专属BOM树形可视区域计算算法，预计算所有节点高度、折叠偏移量、可视索引映射关系，仅渲染屏幕可视节点，DOM数量恒定40–80个，彻底解决十万级节点渲染卡顿问题。

### 7\.2 BOM树形增量Diff算法

精准对比新旧树形数据，最小粒度识别新增、删除、移动、层级变更、字段修改，仅更新变更区域，杜绝全局重渲染。

### 7\.3 事务化撤销重做算法

批量操作、层级调整、批量编辑自动合并为单一事务，无上限事务栈存储，支持任意步数回退与恢复，视图切换不清空操作记录。

### 7\.4 拓扑循环依赖检测算法

O\(n\)线性时间复杂度遍历，快速检测BOM闭环依赖问题，精准定位异常链路，适配工业级严苛校验场景。

## 8\. Mock Demo验证体系（全能力验收载体）

独立Mock Demo工程，用于**性能量化验收、定制化能力验证、交互校验、AI能力演示**，所有功能必须可可视化复现。

### 8\.1 四大核心验证页面

1. **性能压测页**：1w/5w/10w级模拟数据切换，实时展示FPS、内存、渲染耗时，自动化性能验收

2. **自定义列演示页**：完整演示列配置、模板保存、自定义字段、插槽自定义单元格/列头

3. **快捷键与交互演示页**：默认快捷键、自定义快捷键、批量操作、原位编辑全场景演示

4. **AI智能能力页**：BOM生成、结构化重构、纠错校验、结构优化全功能演示

### 8\.2 Mock数据生成规范

- 自动生成3–8级多层嵌套标准BOM结构

- 内置正常数据、错乱层级、循环依赖、重复冗余等异常测试数据

- 支持一键重置、一键生成超大批量测试数据

## 9\. 组件API与扩展规范

### 9\.1 接入规范

极简接入、零配置可用，支持CDN、NPM、项目本地引入三种方式，适配Vue3/React全框架。

### 9\.2 扩展能力

- 插槽扩展：自定义列头、单元格、行样式、行操作按钮、空状态

- 事件系统：全覆盖增删改、层级变更、选中、折叠、校验失败、AI操作事件

- 钩子函数：覆盖组件初始化、更新、销毁、数据变更全生命周期

- 快捷键API：支持全局自定义、局部覆盖、按键禁用

## 10\. 生产级验收标准（全部通过方可上线）

### 10\.1 性能验收标准

- 十万级节点流畅渲染，帧率稳定50fps\+

- 所有耗时计算Worker异步化，主线程零阻塞

- 虚拟滚动无白屏、无抖动、无错位

- 增量更新生效，无全局无效重渲染

### 10\.2 交互验收标准

- 全部默认快捷键正常生效，支持自定义修改、冲突拦截、恢复默认

- 拖拽、编辑、展开折叠零延迟跟手

- 撤销重做完整可靠，所有操作可回溯

- 视图状态缓存生效，切换页面不重置操作状态

### 10\.3 定制化验收标准

- 列可视化配置全功能可用，支持排序、显隐、固定、宽高调整

- 多套列模板可保存、切换、持久化

- 自定义字段、自定义单元格、自定义列头完全生效

### 10\.4 智能化验收标准

- 自然语言可生成标准多层级BOM结构

- 自动识别循环依赖、层级错误、数据异常并修复

- AI自动规整、优化BOM结构，完成数据汇总统计

## 11\. 工程化落地规范

- 组件独立打包，支持Tree\-Shaking、按需引入，体积轻量化

- 全量TypeScript严格类型覆盖，无隐式任意类型

- 零DOM泄漏、零事件残留、零内存累积，长期运行稳定

- 兼容Chrome、Edge、国产信创浏览器全环境

- 支持Web嵌入、系统集成、私有化离线部署，适配工业业务场景

> （注：部分内容可能由 AI 生成）
